package com.connecter.digitalguiljabiback.repository.board;

import com.connecter.digitalguiljabiback.domain.board.BoardVersionContent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoardVersionContentRepository extends JpaRepository<BoardVersionContent, Long> {
}
